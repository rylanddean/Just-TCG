import SwiftUI

struct AnalyticsView: View {
    var body: some View {
        NavigationStack {
            Text("Analytics")
                .foregroundStyle(.secondary)
                .navigationTitle("Analytics")
        }
    }
}
