import SwiftUI

struct CardsView: View {
    var body: some View {
        NavigationStack {
            Text("Cards")
                .foregroundStyle(.secondary)
                .navigationTitle("Cards")
        }
    }
}
