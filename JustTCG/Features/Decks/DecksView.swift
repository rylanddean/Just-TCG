import SwiftUI

struct DecksView: View {
    var body: some View {
        NavigationStack {
            Text("Decks")
                .foregroundStyle(.secondary)
                .navigationTitle("Decks")
        }
    }
}
