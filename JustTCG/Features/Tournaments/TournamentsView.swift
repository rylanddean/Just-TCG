import SwiftUI

struct TournamentsView: View {
    var body: some View {
        NavigationStack {
            Text("Tournaments")
                .foregroundStyle(.secondary)
                .navigationTitle("Tournaments")
        }
    }
}
